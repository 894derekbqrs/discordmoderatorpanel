<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Moderator Panel — VOICE CHAT DEMO (Not Real)</title>
  <style>
    :root{
      --bg:#0e1116;
      --panel:#0f1720;
      --muted:#9aa4b2;
      --accent:#6b9cff;
      --danger:#ff6b6b;
      --card:#11161b;
      --glass: rgba(255,255,255,0.03);
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      background:linear-gradient(180deg, #081019 0%, #06121a 100%);
      color:#dbe7ff;
      font-size:14px;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:24px;
    }
    .app{
      width:1100px;
      max-width:100%;
      display:grid;
      grid-template-columns: 260px 1fr 320px;
      gap:18px;
      align-items:stretch;
    }
    header.demo-banner{
      grid-column: 1 / -1;
      background:linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border:1px solid rgba(255,255,255,0.03);
      color:var(--muted);
      padding:10px 14px;
      border-radius:10px;
      display:flex;
      justify-content:space-between;
      align-items:center;
      margin-bottom:6px;
    }
    .badge{
      background:linear-gradient(90deg, rgba(107,156,255,0.12), rgba(107,156,255,0.06));
      color:var(--accent);
      padding:6px 10px;
      border-radius:999px;
      font-weight:600;
      font-size:13px;
      letter-spacing:0.2px;
    }

    .card{
      background:var(--card);
      border-radius:12px;
      padding:12px;
      border:1px solid rgba(255,255,255,0.03);
      box-shadow: 0 6px 20px rgba(2,6,23,0.6);
      height:100%;
      display:flex;
      flex-direction:column;
      gap:10px;
    }

    /* Left sidebar */
    .sidebar h3{margin:0 0 6px 0; color:var(--muted); font-size:13px}
    .server{
      display:flex;
      align-items:center;
      gap:10px;
      margin-bottom:6px;
    }
    .server .logo{
      width:36px;height:36px;border-radius:8px;background:linear-gradient(180deg,#2b3340,#11161b);display:flex;align-items:center;justify-content:center;font-weight:700;color:#9fb3ff
    }
    .channels{overflow:auto;padding-right:6px}
    .channel{
      background:var(--glass);
      padding:8px 10px;border-radius:8px;margin-bottom:8px;display:flex;align-items:center;justify-content:space-between;
      cursor:default;
    }
    .channel .meta{display:flex;gap:8px;align-items:center}
    .channel .name{font-weight:600;color:#e6f0ff}
    .channel small{color:var(--muted);font-size:12px}

    /* Main */
    .vc-header{display:flex;justify-content:space-between;align-items:center}
    .vc-title{font-size:18px;font-weight:700}
    .controls{display:flex;gap:8px}
    button.btn{
      padding:8px 10px;border-radius:8px;border:0;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01)); color:var(--muted);cursor:pointer;font-weight:600;
    }
    button.btn.primary{color:white;background:linear-gradient(180deg,var(--accent),#2f6fe3);box-shadow:0 6px 14px rgba(48,91,196,0.18)}
    button.btn.warn{background:linear-gradient(180deg,#ffb3b3,#ff8b8b); color:#550000}
    .participants{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:12px;overflow:auto;padding-right:6px}

    .participant{
      background:linear-gradient(180deg, rgba(255,255,255,0.015), rgba(0,0,0,0.02));
      border-radius:10px;padding:10px;display:flex;gap:10px;align-items:center;justify-content:space-between;border:1px solid rgba(255,255,255,0.02)
    }
    .p-left{display:flex;gap:10px;align-items:center}
    .avatar{width:48px;height:48px;border-radius:10px;background:#18202a;display:flex;align-items:center;justify-content:center;font-weight:700;color:#b7d2ff}
    .p-info{min-width:0}
    .p-info .name{font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
    .p-info .meta{color:var(--muted);font-size:12px;margin-top:4px}

    .p-actions{display:flex;gap:6px}
    .icon-btn{padding:8px;border-radius:8px;border:0;background:transparent;color:var(--muted);cursor:pointer;font-weight:700}
    .icon-btn.active{background:rgba(255,255,255,0.03);color:white}

    /* Right column */
    .log{flex:1;overflow:auto;font-family:monospace;background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.006));padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.02);height:100%}
    .log p{margin:6px 0;color:#cfe0ff;font-size:13px}

    footer.attribution{color:var(--muted);font-size:12px;text-align:center;margin-top:6px}

    /* small screens */
    @media (max-width:980px){
      .app{grid-template-columns:1fr; padding:0
